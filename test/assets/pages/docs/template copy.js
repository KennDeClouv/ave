import { setupPage } from "../setupPage.js";

export function Page() {
  const title = "Layout";
  const sub = ""
  const id ="to Page"
  const content = `

  `;
  setupPage(title, sub, content, id);
}

